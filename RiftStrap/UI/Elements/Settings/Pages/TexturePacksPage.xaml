<ui:UiPage x:Class="RiftStrap.UI.Elements.Settings.Pages.TexturePacksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:rift="clr-namespace:RiftStrap.UI.Controls.Rift"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="700"
      Scrollable="True"
      Loaded="OnPageLoaded">

    <ui:UiPage.Resources>
        <Storyboard x:Key="SectionEntrance">
            <DoubleAnimation Storyboard.TargetName="S1" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0" />
            <DoubleAnimation Storyboard.TargetName="S1T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="S2" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0.08" />
            <DoubleAnimation Storyboard.TargetName="S2T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0.08">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="S3" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0.16" />
            <DoubleAnimation Storyboard.TargetName="S3T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0.16">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </ui:UiPage.Resources>

    <Grid Margin="8,0,20,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- S1: Title row with accent line + action buttons -->
        <StackPanel x:Name="S1" Grid.Row="0" Opacity="0" Margin="0,0,0,18">
            <StackPanel.RenderTransform><TranslateTransform x:Name="S1T" /></StackPanel.RenderTransform>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Texture Packs" FontSize="22" FontWeight="SemiBold" Foreground="#FAFAFA" VerticalAlignment="Center" />
                    <Border Width="32" Height="2" Background="#333333" VerticalAlignment="Center" Margin="14,2,14,0" CornerRadius="1" />
                    <TextBlock Text="ASSET BROWSER" FontSize="9" FontWeight="SemiBold" Foreground="#2A2A2A" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <rift:RiftButton Content="Install ZIP" Variant="Primary" Click="InstallZip_Click" Margin="0,0,6,0" />
                    <rift:RiftButton Content="Install Folder" Variant="Ghost" Click="InstallFolder_Click" />
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- S2: Active pack strip -->
        <Grid x:Name="S2" Grid.Row="1" Opacity="0" Margin="0,0,0,16">
            <Grid.RenderTransform><TranslateTransform x:Name="S2T" /></Grid.RenderTransform>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="ACTIVE" FontSize="9" Foreground="#2A2A2A" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0" />

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="ActivePackName" Text="None" FontSize="15" FontWeight="SemiBold" Foreground="#FAFAFA" VerticalAlignment="Center" />
                    <TextBlock Text=" / " FontSize="13" Foreground="#333333" VerticalAlignment="Center" />
                    <TextBlock x:Name="ActivePackInfo" Text="No texture pack applied" FontSize="12" Foreground="#555555" VerticalAlignment="Center" />
                </StackPanel>

                <rift:RiftButton x:Name="RemoveActiveButton" Grid.Column="2" Content="Remove" Variant="Ghost" Click="RemoveActive_Click" VerticalAlignment="Center" Visibility="Collapsed" Padding="8,4" FontSize="11" />
            </Grid>

            <Border Height="1" Background="#0CFFFFFF" VerticalAlignment="Bottom" Margin="0,10,0,0" />
        </Grid>

        <!-- S3: Packs list + empty state -->
        <Grid x:Name="S3" Grid.Row="2" Opacity="0">
            <Grid.RenderTransform><TranslateTransform x:Name="S3T" /></Grid.RenderTransform>

            <ItemsControl x:Name="PacksList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,6" Padding="14,10" CornerRadius="10" Background="#06FFFFFF">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="#EEEEEE" />
                                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                        <TextBlock FontSize="10" Foreground="#444444">
                                            <Run Text="by" />
                                            <Run Text="{Binding Author}" />
                                        </TextBlock>
                                        <TextBlock Text=" / " FontSize="10" Foreground="#222222" />
                                        <TextBlock Text="{Binding FileCountText}" FontSize="10" Foreground="#333333" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <rift:RiftButton Content="Apply" Variant="Secondary" Click="ApplyPack_Click" Tag="{Binding Id}" Padding="10,5" FontSize="11" Margin="0,0,4,0" />
                                    <rift:RiftButton Content="Uninstall" Variant="Ghost" Click="UninstallPack_Click" Tag="{Binding Id}" Padding="10,5" FontSize="11" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Empty state -->
            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                <TextBlock Text="No texture packs installed" FontSize="15" FontWeight="Medium" Foreground="#555555" HorizontalAlignment="Center" />
                <TextBlock Text="Install a .zip or folder containing replacement textures" FontSize="12" Foreground="#333333" HorizontalAlignment="Center" Margin="0,6,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:UiPage>
