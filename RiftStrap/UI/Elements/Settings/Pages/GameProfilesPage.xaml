<ui:UiPage x:Class="RiftStrap.UI.Elements.Settings.Pages.GameProfilesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:rift="clr-namespace:RiftStrap.UI.Controls.Rift"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="700"
      Scrollable="True"
      Loaded="OnPageLoaded">

    <ui:UiPage.Resources>
        <Storyboard x:Key="SectionEntrance">
            <DoubleAnimation Storyboard.TargetName="S1" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0" />
            <DoubleAnimation Storyboard.TargetName="S1T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="S2" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0.08" />
            <DoubleAnimation Storyboard.TargetName="S2T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0.08">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="S3" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0.16" />
            <DoubleAnimation Storyboard.TargetName="S3T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0.16">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="S4" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0.24" />
            <DoubleAnimation Storyboard.TargetName="S4T" Storyboard.TargetProperty="Y" From="18" To="0" Duration="0:0:0.5" BeginTime="0:0:0.24">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </ui:UiPage.Resources>

    <Grid Margin="8,0,20,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- S1: Title with accent line + Add button right-aligned -->
        <Grid x:Name="S1" Grid.Row="0" Opacity="0" Margin="0,0,0,18">
            <Grid.RenderTransform><TranslateTransform x:Name="S1T" /></Grid.RenderTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Game Profiles" FontSize="22" FontWeight="SemiBold" Foreground="#FAFAFA" VerticalAlignment="Center" />
            <Border Grid.Column="1" Width="32" Height="2" Background="#333333" VerticalAlignment="Center" Margin="14,2,0,0" CornerRadius="1" />
            <rift:RiftButton Grid.Column="3" Content="Add Profile" Variant="Primary" Click="AddProfile_Click" VerticalAlignment="Center" />
        </Grid>

        <!-- S2: Auto-detect toggle as inline row (no card wrapper) -->
        <Grid x:Name="S2" Grid.Row="1" Opacity="0" Margin="0,0,0,16">
            <Grid.RenderTransform><TranslateTransform x:Name="S2T" /></Grid.RenderTransform>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Auto-Detect Games" FontSize="14" FontWeight="Medium" Foreground="#DDDDDD" />
                    <TextBlock Text="Automatically apply profiles when joining" FontSize="11" Foreground="#444444" Margin="0,3,0,0" />
                </StackPanel>

                <rift:RiftToggle x:Name="AutoDetectToggle" Grid.Column="1" VerticalAlignment="Center" Checked="AutoDetect_Changed" Unchecked="AutoDetect_Changed" />
            </Grid>

            <!-- Thin divider -->
            <Border Height="1" Background="#0CFFFFFF" VerticalAlignment="Bottom" />
        </Grid>

        <!-- S3: Profiles as clean rows with thin bottom dividers -->
        <Grid x:Name="S3" Grid.Row="2" Opacity="0">
            <Grid.RenderTransform><TranslateTransform x:Name="S3T" /></Grid.RenderTransform>

            <ItemsControl x:Name="ProfilesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Grid Margin="0,10,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Info -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="#EEEEEE" />
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock FontSize="10" Foreground="#444444">
                                            <Run Text="Place:" />
                                            <Run Text="{Binding PlaceId}" />
                                        </TextBlock>
                                        <TextBlock Text=" / " FontSize="10" Foreground="#222222" Margin="4,0" />
                                        <TextBlock FontSize="10" Foreground="#444444">
                                            <Run Text="Flags:" />
                                            <Run Text="{Binding FastFlags.Count}" />
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Toggle enabled -->
                                <rift:RiftToggle Grid.Column="1" IsChecked="{Binding Enabled}" VerticalAlignment="Center" Margin="0,0,12,0" />

                                <!-- Delete -->
                                <rift:RiftButton Grid.Column="2" Content="X" Variant="Ghost" VerticalAlignment="Center" Padding="6,4" FontSize="11" Click="DeleteProfile_Click" Tag="{Binding PlaceId}" />
                            </Grid>

                            <!-- Thin divider between rows -->
                            <Border Height="1" Background="#08FFFFFF" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!-- S4: Empty state -->
        <StackPanel x:Name="S4" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Visible"
                    Opacity="0">
            <StackPanel.RenderTransform><TranslateTransform x:Name="S4T" /></StackPanel.RenderTransform>
            <TextBlock Text="No game profiles" FontSize="15" FontWeight="Medium" Foreground="#555555" HorizontalAlignment="Center" />
            <TextBlock Text="Create a profile to auto-switch settings per game" FontSize="12" Foreground="#333333" HorizontalAlignment="Center" Margin="0,6,0,0" />
        </StackPanel>
    </Grid>
</ui:UiPage>
