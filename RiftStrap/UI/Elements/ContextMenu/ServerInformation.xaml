<base:WpfUiWindow x:Class="RiftStrap.UI.Elements.ContextMenu.ServerInformation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RiftStrap.UI.Elements.ContextMenu"
        xmlns:base="clr-namespace:RiftStrap.UI.Elements.Base"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:models="clr-namespace:RiftStrap.UI.ViewModels.ContextMenu"
        xmlns:resources="clr-namespace:RiftStrap.Resources"
        d:DataContext="{d:DesignInstance Type=models:ServerInformationViewModel}"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}"
        MinWidth="0"
        MinHeight="0"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border CornerRadius="16" Background="#111111" BorderBrush="#1AFFFFFF" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Border Grid.Row="0" Padding="20,14,14,10">
                <Grid>
                    <TextBlock Text="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="#FAFAFA" />
                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseBtn_Click"
                            Width="28" Height="28" Padding="0">
                        <ui:SymbolIcon Symbol="Dismiss24" FontSize="14" Foreground="#888888" />
                    </Button>
                </Grid>
            </Border>

            <!-- Server info in gradient card -->
            <Border Grid.Row="1" CornerRadius="14" Margin="12,0,12,12" Padding="18,16" ClipToBounds="True">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0AFFFFFF" Offset="0" />
                        <GradientStop Color="#05FFFFFF" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,16,12" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_Type}" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding ServerType, Mode=OneWay}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,16,12" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_InstanceId}" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding InstanceId, Mode=OneWay}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,16,12" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_Location}" Visibility="{Binding ServerLocationVisibility, Mode=OneTime}" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding ServerLocation, Mode=OneWay}" Visibility="{Binding ServerLocationVisibility, Mode=OneTime}" />
                </Grid>
            </Border>

            <!-- Footer buttons -->
            <Border Grid.Row="2" Padding="15" CornerRadius="0,0,16,16" Background="#0AFFFFFF">
                <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
                    <Button MinWidth="100" Content="{x:Static resources:Strings.ContextMenu_ServerInformation_CopyInstanceId}" Command="{Binding CopyInstanceIdCommand, Mode=OneTime}" />
                    <Button Margin="12,0,0,0" MinWidth="100" Content="{x:Static resources:Strings.Common_Close}" Click="CloseBtn_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</base:WpfUiWindow>
