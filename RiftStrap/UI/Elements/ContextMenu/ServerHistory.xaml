<base:WpfUiWindow x:Class="RiftStrap.UI.Elements.ContextMenu.ServerHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RiftStrap.UI.Elements.ContextMenu"
        xmlns:base="clr-namespace:RiftStrap.UI.Elements.Base"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:models="clr-namespace:RiftStrap.UI.ViewModels.ContextMenu"
        xmlns:resources="clr-namespace:RiftStrap.Resources"
        xmlns:enums="clr-namespace:RiftStrap.Enums"
        d:DataContext="{d:DesignInstance Type=models:ServerHistoryViewModel}"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}"
        MinWidth="420"
        MinHeight="420"
        Width="580"
        Height="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border CornerRadius="16" Background="#111111" BorderBrush="#1AFFFFFF" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Border Grid.Row="0" Padding="20,14,14,10">
                <Grid>
                    <TextBlock Text="{x:Static resources:Strings.ContextMenu_GameHistory_Title}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="#FAFAFA" />
                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseBtn_Click"
                            Width="28" Height="28" Padding="0">
                        <ui:SymbolIcon Symbol="Dismiss24" FontSize="14" Foreground="#888888" />
                    </Button>
                </Grid>
            </Border>

            <!-- Description -->
            <TextBlock Grid.Row="1" Margin="20,0,20,8" Text="{x:Static resources:Strings.ContextMenu_GameHistory_Description}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />

            <!-- Content area -->
            <Grid Grid.Row="2" Margin="12,0,12,12">
                <!-- Error state -->
                <TextBlock Margin="8,8,8,0" Text="{Binding Error, Mode=OneWay}" TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Failed}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Loading state -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Unknown}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </Border.Style>
                    <ui:ProgressRing IsIndeterminate="True" />
                </Border>

                <!-- History list in gradient card -->
                <Border CornerRadius="14" Padding="6" ClipToBounds="True">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#0AFFFFFF" Offset="0" />
                            <GradientStop Color="#05FFFFFF" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <ListView ItemsSource="{Binding GameHistory, Mode=OneWay}" Margin="0">
                        <ListView.Style>
                            <Style TargetType="ListView" BasedOn="{StaticResource {x:Type ListView}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Successful}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Style>
                        </ListView.Style>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Type ListViewItem}}">
                                <Setter Property="Focusable" Value="False" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ui:Card Padding="0">
                                    <Grid VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Width="107" Height="107" CornerRadius="4">
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding UniverseDetails.Thumbnail.ImageUrl, IsAsync=True}" />
                                            </Border.Background>
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="16,12,12,12" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding UniverseDetails.Data.Name}" FontSize="18" FontWeight="Medium" />
                                            <TextBlock Text="{Binding GameHistoryDescription}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                            <ui:Button Margin="0,8,0,0" Content="{x:Static resources:Strings.ContextMenu_GameHistory_Rejoin}" Command="{Binding RejoinServerCommand}"
                                                       Appearance="Success" Foreground="White" IconForeground="White" Icon="Play28" IconFilled="True" />
                                        </StackPanel>
                                    </Grid>
                                </ui:Card>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>
        </Grid>
    </Border>
</base:WpfUiWindow>
