<ui:UiPage x:Class="RiftStrap.UI.Elements.Installer.Pages.InstallPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:RiftStrap.Resources"
      xmlns:local="clr-namespace:RiftStrap.UI.Elements.Installer.Pages"
      xmlns:controls="clr-namespace:RiftStrap.UI.Elements.Controls"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InstallPage"
      Scrollable="True"
      Loaded="UiPage_Loaded">

    <StackPanel Margin="0,0,14,14">
        <!-- Install Location section -->
        <TextBlock FontSize="13" FontWeight="Medium" Foreground="#999999"
                   Text="{x:Static resources:Strings.Installer_Install_Location_Title}" TextWrapping="Wrap" />
        <TextBlock FontSize="12" Foreground="#555555" Margin="0,4,0,0"
                   Text="{x:Static resources:Strings.Installer_Install_Location_Text}" TextWrapping="Wrap" />

        <!-- Path input row -->
        <Border Margin="0,10,0,0" Padding="10" CornerRadius="6" Background="#0A0A0A" BorderBrush="#0AFFFFFF" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Margin="0,0,6,0" VerticalAlignment="Center"
                         Text="{Binding InstallLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ui:Button Grid.Column="1" Margin="4,0,4,0" Height="33" Icon="Folder24"
                           Content="{x:Static resources:Strings.Common_Browse}"
                           Command="{Binding BrowseInstallLocationCommand}" />
                <ui:Button Grid.Column="2" Margin="4,0,0,0" Height="33" Icon="ArrowCounterclockwise24"
                           Content="{x:Static resources:Strings.Common_Reset}"
                           Command="{Binding ResetInstallLocationCommand}" />
            </Grid>
        </Border>

        <!-- Data found message -->
        <TextBlock Margin="0,8,0,0" FontSize="12" Foreground="#666666"
                   Text="{x:Static resources:Strings.Installer_Install_Location_DataFound}"
                   Visibility="{Binding DataFoundMessageVisibility, Mode=OneWay}" TextWrapping="Wrap" />

        <!-- Error message -->
        <TextBlock Margin="0,8,0,0" FontSize="12"
                   Text="{Binding ErrorMessage, Mode=OneWay}"
                   Foreground="{DynamicResource SystemFillColorCriticalBrush}" TextWrapping="Wrap">
            <TextBlock.Style>
                <Style>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=Self},Path=Text}" Value="">
                            <Setter Property="UIElement.Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Separator -->
        <Border Height="1" Background="#0AFFFFFF" Margin="0,18,0,14" />

        <!-- Shortcuts section -->
        <TextBlock FontSize="13" FontWeight="Medium" Foreground="#999999"
                   Text="{x:Static resources:Strings.Common_Shortcuts}" TextWrapping="Wrap" />
        <TextBlock FontSize="12" Foreground="#555555" Margin="0,4,0,0"
                   Text="{x:Static resources:Strings.Installer_Install_Shortcuts_Description}" TextWrapping="Wrap" />

        <Grid Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl Grid.Column="0" Margin="0,0,4,0"
                Header="{x:Static resources:Strings.Common_Shortcuts_Desktop}">
                <ui:ToggleSwitch IsChecked="{Binding CreateDesktopShortcuts, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl Grid.Column="1" Margin="4,0,0,0"
            Header="{x:Static resources:Strings.Common_Shortcuts_StartMenu}">
                <ui:ToggleSwitch IsChecked="{Binding CreateStartMenuShortcuts, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>

        <!-- Separator -->
        <Border Height="1" Background="#0AFFFFFF" Margin="0,18,0,14" />

        <!-- Analytics section -->
        <TextBlock FontSize="13" FontWeight="Medium" Foreground="#999999"
                   Text="{x:Static resources:Strings.Common_Analytics}" TextWrapping="Wrap" />
        <controls:MarkdownTextBlock FontSize="12" Foreground="#555555" Margin="0,4,0,0"
            MarkdownText="{Binding Source={x:Static resources:Strings.Menu_RiftStrap_Analytics_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://riftstrap.com/wiki/info/privacy-policy/'}"
            TextWrapping="Wrap" />

        <controls:OptionControl Margin="0,4,0,0"
            Header="{x:Static resources:Strings.Menu_RiftStrap_Analytics_Title}">
            <ui:ToggleSwitch IsChecked="{Binding AnalyticsEnabled, Mode=TwoWay}" />
        </controls:OptionControl>
    </StackPanel>
</ui:UiPage>
