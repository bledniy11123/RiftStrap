<ui:UiPage x:Class="RiftStrap.UI.Elements.Installer.Pages.CompletionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:RiftStrap.Resources"
      xmlns:local="clr-namespace:RiftStrap.UI.Elements.Installer.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CompletionPage"
      Scrollable="True"
      Loaded="UiPage_Loaded">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="400" Margin="0,32,0,20">
            <!-- Success checkmark -->
            <Border Width="56" Height="56" CornerRadius="28" HorizontalAlignment="Center" Margin="0,0,0,20"
                    Background="#08FFFFFF">
                <ui:SymbolIcon Symbol="CheckmarkCircle48" FontSize="28" Foreground="#C0C0C0"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>

            <!-- Ready text -->
            <TextBlock FontSize="14" Foreground="#AAAAAA" HorizontalAlignment="Center" TextAlignment="Center"
                       Text="{x:Static resources:Strings.Installer_Completion_Text}" TextWrapping="Wrap"
                       Margin="0,0,0,28" />

            <!-- Action buttons -->
            <ui:CardAction Margin="0,0,0,0" Icon="Settings28" Command="{Binding LaunchSettingsCommand, Mode=OneTime}">
                <StackPanel>
                    <TextBlock FontSize="13" Foreground="#CCCCCC"
                               Text="{x:Static resources:Strings.Installer_Completion_Settings_Title}" />
                    <TextBlock Margin="0,2,0,0" FontSize="11" Padding="0,0,16,0" Foreground="#555555"
                               Text="{x:Static resources:Strings.Installer_Completion_Settings_Description}" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Margin="0,8,0,0" Icon="ArrowRight12" Command="{Binding LaunchRobloxCommand, Mode=OneTime}">
                <StackPanel>
                    <TextBlock FontSize="13" Foreground="#CCCCCC"
                               Text="{x:Static resources:Strings.Installer_Completion_Launch_Title}" />
                    <TextBlock Margin="0,2,0,0" FontSize="11" Padding="0,0,16,0" Foreground="#555555"
                               Text="{x:Static resources:Strings.Installer_Completion_Launch_Description}" />
                </StackPanel>
            </ui:CardAction>
        </StackPanel>
    </Grid>
</ui:UiPage>
